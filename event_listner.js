// we use event listener over writing function because we can do multiple event listener but not function

// Button.onclick( () => {

// })

// Button.onclick( () => {
    
// })   =>    2nd one will perform


Button.addEventListener("click",(eve) => {

})

// for removeEventListener we will save function in var and pass it to the eventListener